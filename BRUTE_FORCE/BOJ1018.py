import sys
n, m = map(int, sys.stdin.readline())
chess = []
for i in range(n):
    chess.append([sys.stdin.readline().rstrip()])
for j in range(len(chess)):
    if chess[j][0] == 'w':
        chess[j]


